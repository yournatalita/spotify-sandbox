import * as React from 'react';
import Start from '../ui/components/Start/Start';

const Home = (): any => {
  return (
    <div className="Home">
      <Start />
    </div>
  );
};

export default Home;
